# frps-gateway 项目说明

## 项目定位

为 frps 提供可控、可审计的来源 IP 访问控制。用户通过飞书申请访问权限，系统将 IP、飞书用户、申请时间和过期时间关联保存，并由 frps 在连接入口执行拦截。

## 核心原则

- 默认拒绝：启用白名单后，未授权来源不能访问 TCP 类映射服务。
- 最小权限：授权使用 TTL，避免永久开放端口。
- 控制面与数据面分离：机器人和网页只调用 frps 管理 API，连接校验在 frps 本地内存中完成。
- 可回退：关闭白名单开关即可恢复原有转发行为。
- 可审计：记录操作者、IP、操作类型、时间和结果。

## 主要能力

1. frps 全局 IP allowlist 与过期清理。
2. `GET/POST/DELETE /api/v2/whitelist` 管理接口，复用 dashboard Basic Auth。
3. 飞书机器人通过 `/start` 动态卡片提供申请、查询和撤销访问操作。
4. 网页查看白名单、操作者和时间信息。
5. 一次性授权链接自动获取用户公网 IP。

## 非目标

首期不支持 CIDR、UDP/SUDP 来源拦截、白名单持久化和按代理设置不同白名单。

## 重要约束

自动识别 IP 必须依赖用户访问授权网页；飞书长连接本身只能识别飞书用户身份，不能识别用户公网出口 IP。

## Phase 1 架构决策

### 仓库模型

- `frp` 与 `frps-gateway` 保持两个独立 Git 仓库。
- `frp` 作为定制 fork，承载连接拦截、持久化白名单、短期变更审计和管理 API。
- `frps-gateway` 承载飞书、审计、恢复、网页和项目级文档。
- `frps-gateway` 使用 `main` 作为稳定主分支；功能开发使用短生命周期分支并通过测试后合并。

### 数据归属

- frps 保存当前生效的标准化 IP、过期时间、访问时段、管理账号元数据和最近 5000 次成功变更，但不保存飞书用户资料或消息上下文。
- frps-gateway 使用 SQLite 保存用户、申请、操作结果、访问记录和后台操作审计。
- gateway 是审计和恢复来源；frps 重启后，gateway 将仍未过期的有效授权重新同步到 frps。
- frps 白名单功能启用且当前名单为空时采用 fail-closed，拒绝所有受支持的业务连接。
- 管理 API 不受业务端口白名单限制，因此即使业务访问被拒绝，gateway 仍可恢复规则。
- 重复添加同一 IP 视为刷新授权，从服务器当前时间重新计算过期时间，并产生新的审计记录。
- gateway `/admin` 是日常授权和长期审计入口；frps `/whitelist` 仅用于应急操作。frps 手工条目在 gateway 中只读展示，不自动接管或删除。

### API 与网络边界

- 白名单接口复用 frps dashboard API v2 的 `{code,msg,data}` 响应结构。
- 管理接口使用 dashboard Basic Auth，只允许本机或受控管理内网访问。
- 用户和审计字段不进入 frps API，由 gateway 在调用 API 前后记录。
- 冻结后的详细契约见 [`docs/api.md`](./docs/api.md)。
