# frps-gateway Roadmap

本文档用于记录项目当前进度、任务拆解、依赖顺序、版本里程碑和各阶段完成标准。详细功能需求见 [`docs/ip-whitelist-requirements.md`](./docs/ip-whitelist-requirements.md)。

## 当前状态

当前项目处于“需求已明确、飞书机器人原型基本完成、frps 核心改造尚未开始”的阶段，按最终上线目标估算整体进度约为 20%～25%。

| 模块 | 参考进度 | 状态 |
|---|---:|---|
| 项目定位与需求 | 90% | 项目说明、路线图和验收目标已形成 |
| 飞书机器人基础能力 | 70% | 指令解析、管理员校验、长连接和回复已完成 |
| frps API 客户端 | 60% | 客户端和模拟测试已完成，服务端接口尚不存在 |
| frps 白名单核心 | 0% | 配置、内存存储和连接拦截尚未实现 |
| frps 管理 API | 0% | `/api/v2/whitelist` 尚未注册，契约状态为 `Draft` |
| 用户与审计数据 | 0% | 尚无数据模型和持久化存储 |
| 网页管理 | 0% | 尚无 Web 服务、页面和登录鉴权 |
| 自动识别 IP | 0% | 一次性授权链接尚未实现 |
| 部署与安全加固 | 0% | 尚无正式部署和上线验证 |

当前已通过 `command`、`config`、`duration`、`executor` 和 `frps` 客户端的单元测试；`feishu` 事件处理和真实端到端链路仍缺少测试。

## 开发原则

- 先提供可用的管理 API，再启用连接拦截，避免出现无法添加管理 IP 的锁死状态。
- frps 只负责当前生效的访问规则和高性能连接校验；用户、申请和审计数据由 frps-gateway 管理。
- 白名单功能默认关闭，关闭后必须保持与原版 frps 一致。
- 安全要求跟随对应功能实现，不集中拖到最后阶段。
- 每个阶段必须满足完成标准后才能进入下一阶段。

## 版本里程碑

| 版本 | 交付目标 |
|---|---|
| `v0.1.0` | 架构与 API 契约确定，完成 frps 白名单存储和管理 API |
| `v0.2.0` | 完成连接拦截和飞书机器人 MVP 闭环 |
| `v0.3.0` | 完成 SQLite 审计、用户关联和网页管理 |
| `v0.4.0` | 完成一次性链接和用户公网 IP 自动识别 |
| `v1.0.0` | 完成部署、安全、故障恢复和正式上线验收 |

## Phase 0：项目基础整理（已完成）

- [x] 统一项目名称为 `frps-gateway`
- [x] 整理项目说明、路线图和白名单需求文档
- [x] 完成飞书机器人基础代码框架
- [x] 明确飞书长连接无法直接获取用户公网 IP
- [x] 完成机器人核心包的基础单元测试

完成标准：

- 项目名称和 Go module 引用统一。
- 项目范围、核心安全边界和阶段目标已记录。
- 机器人原型可以解析白名单增删查指令并调用模拟 frps API。

## Phase 1：架构决策与 API 契约（已完成）

### 1.1 仓库与文档管理

- [x] 确定 `frp` 与 `frps-gateway` 继续使用两个独立仓库
- [x] 将 `README.md`、`PROJECT.md` 和 `ROADMAP.md` 统一纳入 `frps-gateway` 版本控制
- [x] 使用 `main` 作为稳定主分支并完成 `frps-gateway` 首次提交

### 1.2 数据职责

- [x] 确认 frps 仅保存当前生效的 `IP + expireAt`
- [x] 确认 frps-gateway 使用 SQLite 保存用户、申请和审计记录
- [x] 确定 frps 重启后由 gateway 自动恢复未过期条目
- [x] 确定启用白名单但名单为空时默认拒绝全部受支持连接
- [x] 确定重复添加同一 IP 的行为为刷新有效期并写入新审计记录

### 1.3 API 契约

以下接口当前均为 `Draft`，实现前必须确定请求、响应和错误格式：

| 状态 | 方法与路径 | 用途 |
|---|---|---|
| `Draft` | `GET /api/v2/whitelist` | 查询当前生效的白名单 |
| `Draft` | `POST /api/v2/whitelist` | 添加 IP 或刷新有效期 |
| `Draft` | `DELETE /api/v2/whitelist` | 删除指定 IP |

- [x] 确定成功响应使用 frps v2 `{code,msg,data}` 结构
- [x] 确定 400、401、404 和 500 的使用边界；重复添加不返回 409
- [x] 确定复用 Basic Auth，管理 API 仅允许本机或受控管理内网访问
- [x] 确定列表按标准化 IP 升序排列且不返回过期条目
- [x] 将冻结后的请求、响应和测试方法记录到 `docs/api.md`

### 1.4 审计模型预留

至少预留以下字段：

```text
operatorOpenID
operatorName
chatID
messageID
ip
action
createdAt
expireAt
result
```

- [x] 调整机器人执行接口，使其能够接收操作者和消息上下文
- [x] 明确 frps 只接收 IP 和 TTL，身份及审计字段只保存在 gateway

完成标准：

- 数据归属、重启恢复和默认拒绝策略有明确结论。
- API 契约不存在待实现阶段仍会改变数据模型的关键歧义。
- 项目文件均处于版本控制中。

## Phase 2：frps 白名单存储与管理 API（v0.1.0）

### 2.1 配置与内存存储

- [ ] 增加 `whitelist.enabled` 和 `whitelist.defaultTTL` 配置
- [ ] 实现 IPv4/IPv6 标准化和格式校验
- [ ] 实现并发安全的内存白名单
- [ ] 实现 TTL、惰性过期判断和后台过期清理
- [ ] 实现重复添加刷新有效期
- [ ] 增加配置、TTL、并发和过期单元测试

### 2.2 管理 API

- [ ] 实现 `GET /api/v2/whitelist`
- [ ] 实现 `POST /api/v2/whitelist`
- [ ] 实现 `DELETE /api/v2/whitelist`
- [ ] 复用 dashboard Basic Auth
- [ ] 增加无鉴权、错误鉴权、非法 IP、非法 TTL、删除不存在条目的测试
- [ ] 更新 API 契约状态和请求响应示例

### 2.3 安全启用顺序

- [ ] 保持白名单功能默认关闭
- [ ] 在尚未接入连接拦截前验证 API 可以独立管理白名单
- [ ] 验证管理 API 不受业务端口白名单限制

完成标准：

- API 能够完整执行白名单增删查。
- 非法输入和鉴权失败返回与契约一致的状态码。
- 条目可以自动过期且并发测试通过。
- 此阶段暂不要求阻断用户连接。

## Phase 3：连接拦截与飞书机器人 MVP（v0.2.0）

首个 MVP 范围：

```text
飞书管理员发送明确 IP
→ frps-gateway 调用 frps API
→ IP 临时加入白名单
→ frps 执行连接拦截
→ 飞书返回操作结果
```

### 3.1 frps 连接拦截

- [ ] 将白名单实例注入 `ResourceController`
- [ ] 在 TCP/HTTP/HTTPS/TCPMux 和 visitor 可获取来源 IP 的入口执行校验
- [ ] 在申请 work connection 之前拒绝非法来源
- [ ] 明确 UDP/SUDP 和 XTCP 打洞路径的限制
- [ ] 记录来源 IP、代理名称和拒绝原因
- [ ] 增加允许、拒绝、过期、空名单和关闭开关测试

### 3.2 机器人闭环

- [x] 为 frps API 客户端补充独立单元测试
- [ ] 为飞书事件解析、管理员鉴权和回复补充单元测试
- [x] 将 `open_id`、群 ID 和消息 ID 传入执行层
- [ ] 增加飞书事件去重，避免消息重投导致重复操作
- [ ] 处理 frps 超时、不可用、401、400 和 404
- [ ] 完成真实飞书到真实 frps 的端到端测试

完成标准：

- 非白名单 IP 访问 TCP 类映射端口时被立即拒绝。
- 白名单 IP 可以正常访问，过期后无需重启自动失效。
- `whitelist.enabled = false` 时行为与原版 frps 一致。
- 飞书增删查指令可以在真实环境完成闭环。
- 机器人重复收到相同消息时不会重复写入审计记录。

## Phase 4：用户、审计与网页管理（v0.3.0）

### 4.1 SQLite 数据层

- [ ] 设计用户、授权记录、当前状态和审计日志表
- [ ] 增加数据库初始化和版本迁移
- [ ] 保存操作者、IP、操作类型、时间、有效期和执行结果
- [ ] 处理 gateway 数据与 frps 当前白名单不一致的情况
- [ ] 确定 gateway 重启和 frps 重启后的同步策略

### 4.2 网页管理

- [ ] 增加白名单列表和详情页面
- [ ] 支持按 IP、用户、状态和时间筛选
- [ ] 支持管理员手动添加、删除和延期
- [ ] 增加审计日志页面
- [ ] 增加管理员登录鉴权、会话管理和 CSRF 防护
- [ ] 隐藏密钥和不必要的用户敏感信息

完成标准：

- 网页能够准确显示当前生效白名单及其申请人和时间信息。
- 所有网页变更都产生不可遗漏的审计记录。
- 未认证用户不能读取或修改白名单。
- gateway 数据与 frps 状态能够检测并修复不一致。

## Phase 5：一次性授权链接与自动识别 IP（v0.4.0）

- [ ] 机器人生成绑定飞书用户的一次性随机令牌
- [ ] 令牌具有短有效期、单次使用和防重放能力
- [ ] 用户点击链接后由网页服务识别公网出口 IP
- [ ] 在页面展示识别结果并要求用户确认
- [ ] 将 IP、飞书用户和授权记录关联保存
- [ ] 只信任明确配置的反向代理，安全处理真实客户端 IP
- [ ] 增加令牌过期、重复使用、篡改和并发提交测试
- [ ] 增加授权频率限制和异常行为日志

完成标准：

- 飞书用户无需手工输入 IP 即可完成授权。
- 令牌不可被重复使用或跨用户使用。
- 经过代理部署时仍能安全、准确地识别来源 IP。

## Phase 6：上线加固（v1.0.0）

- [ ] 使用 HTTPS 暴露用户授权网页
- [ ] 将 AppSecret、Basic Auth 密码和签名密钥外置
- [ ] 限制 frps 管理 API 仅允许本机或管理内网访问
- [ ] 增加进程自动启动、健康检查和故障恢复
- [ ] 增加 SQLite 备份和恢复方案
- [ ] 增加审计日志留存和导出策略
- [ ] 完成压力测试、安全测试和故障演练
- [ ] 编写部署、升级、回滚和故障处理文档
- [ ] 在公司飞书租户完成最终验收

完成标准：

- 密钥不以明文提交到代码仓库。
- 管理 API 不直接暴露到公网。
- 服务重启、frps 重启和 gateway 故障均有验证过的恢复路径。
- 所有功能和安全验收项通过后发布 `v1.0.0`。

## 暂不纳入首期范围

- CIDR 网段授权
- UDP/SUDP 来源 IP 拦截
- 按代理维护不同白名单
- XTCP 打洞直连路径拦截
- 飞书卡片交互和审批流
- 多租户管理
